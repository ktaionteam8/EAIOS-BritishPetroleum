// Refiner AI — Dashboard Page (placeholder)
// Full implementation to be built once requirements are finalised.

import React from 'react';

const Dashboard: React.FC = () => (
  <div>
    <h2>Dashboard</h2>
    <p>Global refinery health map, KPI cards, and live status.</p>
  </div>
);

export default Dashboard;
