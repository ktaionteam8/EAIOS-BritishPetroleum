// Refiner AI — Live Alerts Page (placeholder)
import React from 'react';

const LiveAlerts: React.FC = () => (
  <div>
    <h2>Live Alerts</h2>
    <p>AI-prioritised alerts from the LSTM + XGBoost pipeline.</p>
  </div>
);

export default LiveAlerts;
