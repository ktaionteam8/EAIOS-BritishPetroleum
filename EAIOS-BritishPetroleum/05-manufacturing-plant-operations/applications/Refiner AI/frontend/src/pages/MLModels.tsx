// Refiner AI — ML Models Page (placeholder)
import React from 'react';

const MLModels: React.FC = () => (
  <div>
    <h2>ML Models</h2>
    <p>Model registry with accuracy, precision, recall, and retraining history.</p>
  </div>
);

export default MLModels;
